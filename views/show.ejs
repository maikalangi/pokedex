<!DOCTYPE html>
<html lang="en">
<head>
    <%- include ('./partials/head.ejs') %>
    <link rel="stylesheet" href="/css/show.css">
    <title><%= tabTitle %> Stats</title>
</head>
<body class="show-body">
    
    <h1><%= pokemon.name %> Stats</h1>
    <div class="show-container">
        <img id="show-image" src="<%= pokemon.img %> " alt="<%= pokemon.name %> ">
        <ul class="show-list">
            <li class="stats">
            <% for(let i=0; i<pokemon.type.length; i++){ %>
                    <%= pokemon.type[i] %>  
                    <% } %>
                </li>
                <li id="hp">
                    HP:
                    <% if (!pokemon.stats) { %>
                        <%= pokemon.hp %> 
                    <% }else{ %>
                    <%= pokemon.stats.hp %> 
                    <% } %> 
                </li>
                <li id="attack">
                    ATK:
                    <% if (!pokemon.stats) { %>
                        <%= pokemon.hp %> 
                    <% }else{ %>
                    <%= pokemon.stats.attack %> 
                    <% } %> 
                </li>
                <li id="defense">
                    DFS:
                    <% if (!pokemon.stats) { %>
                        <%= pokemon.hp %> 
                    <% }else{ %>
                    <%= pokemon.stats.defense %> 
                    <% } %> 
                </li>
            </ul>
            
        </div>
        <a id="back" href="/pokemon/">Back</a>
</body>
</html>