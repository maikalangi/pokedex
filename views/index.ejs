<!DOCTYPE html>
<html lang="en">
<head>
    <% include ('./partials/head.ejs') %> 
    <title><%= tabTitle %> </title>
</head>
<body>
    <h1>Pokemon</h1>
    <form action="/new/">
        <input type="submit" value="Create New Pokemon!">
    </form>
    <ul>
        <% for (let i=0; i<pokemon.length; i++) { %>
            <li>
                <h2><%= pokemon[i].name %></h2>
                <a href="/<%= i %> ">
                    <img src="<%= pokemon[i].img %>" alt="<%= pokemon.name %>">
                </a><br/>
                <form action="/<%= i %>/edit">
                    <input type="submit" value="EDIT">
                </form>
                <form action="/<%= i %>?_method=DELETE" method="POST">
                    <input type="submit" value="DELETE">
                </form>
            </li>
        <% } %>
    </ul>
</body>
</html>