<!DOCTYPE html>
<html lang="en">
<head>
    <%- include ('./partials/head.ejs') %>
    <link rel="stylesheet" href="/css/index.css">
    <title><%= tabTitle %> </title>
</head>
<body class="index-body">
    <div class="flex">
        <h1>Pokemon</h1>
        <ul id="indexContainer">
            <% for (let i=0; i<pokemon.length; i++) { %>
                <li id="indexItem">
                    <a href="/pokemon/<%= i %> ">
                        <!-- <h2><%# pokemon[i].name %></h2> -->
                        <img class="pic" src="<%= pokemon[i].img %>" alt="<%= pokemon.name %>">
                    </a><br/>
                    <div id="index-buttons">
                        <form id="edit-button" action="/pokemon/<%= i %>/edit">
                            <input type="submit" value="EDIT">
                        </form>
                        <form id="delete-button" action="/pokemon/<%= i %>?_method=DELETE" method="POST">
                            <input type="submit" value="DELETE">
                        </form>
                        
                    </div>
                </li>
                <% } %>
            </ul>
            <form class="inputForm" action="/pokemon/new/">
                <input id="inputButton" type="submit" value="Create New Pokemon!">
            </form>

    </div>
</body>
</html>